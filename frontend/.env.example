# Copy this file to frontend/.env and adjust the values if needed.

# Base URL of the backend API — used by the TypeScript API client
# In production set this to your real API domain, e.g. https://api.example.com
VITE_API_URL=http://localhost:8000

# MailCatcher UI (local development only)
MAILCATCHER_HOST=http://localhost:1080
