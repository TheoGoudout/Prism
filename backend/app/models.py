# This file is superseded by the app/models/ package.
# Python's import system prefers the package over this module.
# It is kept only to avoid git conflicts; all model definitions live in app/models/.
